# IAM policies

`aws-lb-controller.json` is the AWS Load Balancer Controller IAM policy.
Source: https://raw.githubusercontent.com/kubernetes-sigs/aws-load-balancer-controller/v3.2.2/docs/install/iam_policy.json
Refresh on every controller upgrade. Pin to the release tag, never to `main`.
